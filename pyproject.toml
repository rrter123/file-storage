[project]
name = "file-storage"
version = "0.1.0"
description = "File storage app"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pre-commit>=4.6.2",
]

[tool.uv]
package = false
