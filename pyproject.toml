[project]
name = "file-storage"
version = "0.1.0"
description = "File storage app"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
file-storage = "file_storage:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
